@page
@model RazorLibraryWebsite2.Pages.Publishers.DetailsModel

@{
    ViewData["Title"] = "Details";
}
<div class="container">
    <div class="heading text-center">
        <h1>@Model.Publisher.Name  Details</h1>
    </div>

    <div>
        <hr />
        <dl class="row">
            <dt class="col-sm-2">
                @Html.DisplayNameFor(model => model.Publisher.Name)
            </dt>
            <dd class="col-sm-10">
                @Html.DisplayFor(model => model.Publisher.Name)
            </dd>
            <dt class="col-sm-2">
                @Html.DisplayNameFor(model => model.Publisher.Address)
            </dt>
            <dd class="col-sm-10">
                @Html.DisplayFor(model => model.Publisher.Address)
            </dd>
            <dt class="col-sm-2">
                @Html.DisplayNameFor(model => model.Publisher.WebsiteUrl)
            </dt>
            <dd class="col-sm-10">
                @Html.DisplayFor(model => model.Publisher.WebsiteUrl)
            </dd>
        </dl>
    </div>
    <div>
        <a asp-page="./Edit" class="btn btn-primary" asp-route-id="@Model.Publisher.PublisherID">Edit</a> |
        <a asp-page="./Index" class="btn btn-secondary">Back to List</a>
    </div>
    </div>